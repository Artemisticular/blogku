import { Icon } from "astro-icon/components";
import { formatDateToYYYYMMDD } from "../../utils/date-utils";

interface Props {
    title: string;
    description: string;
    doodstreamId: string;
    thumbnail?: string;
    published: Date;
}

const { title, description, doodstreamId, thumbnail, published } = Astro.props;

<div class="card-base p-6 space-y-4">
    <h2 class="text-2xl font-bold">{title}</h2>
    <div class="text-sm text-neutral-500 dark:text-neutral-400">
        Dipublikasikan pada {formatDateToYYYYMMDD(published)}
    </div>
    
    <div class="aspect-video w-full bg-black rounded-lg overflow-hidden">
        <iframe
            src={`https://dood.wf/e/${doodstreamId}`}
            class="w-full h-full"
            allowfullscreen
            loading="lazy"
            referrerpolicy="no-referrer"
        ></iframe>
    </div>

    <div class="prose dark:prose-invert max-w-none">
        <p>{description}</p>
    </div>
</div> 